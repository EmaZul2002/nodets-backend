service: serverless-2024-prova
frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs20.x
  region: us-east-1
  iam:
    role: arn:aws:iam::730335410292:role/LabRole
  
  environment:
    DB_HOST: "uf12-rds.cla48osiutwd.us-east-1.rds.amazonaws.com"
    DB_PORT: 3306
    DB_USER: "admin"
    DB_PASSWORD: "Astore63!"
    DB_NAMEDB: "afp_ps"


functions:
  hello:
    handler: src/handler.hello
    events:
      - httpApi:
          path: /hello
          method: get

  goodbye:
    handler: src/handler.goodbye
    events:
      - httpApi:
          path: /goodbye
          method: get

  getAllPatients:
    handler: src/handler.getAllPatients
    events:
      - httpApi:
          path: /getAllPatients
          method: get

  getPatient:
    handler: src/handler.getPatient
    events:
      - httpApi:
          path: /getPatient
          method: get

plugins:
  - serverless-offline
  - serverless-plugin-typescript